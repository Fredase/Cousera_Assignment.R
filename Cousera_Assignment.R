makeMatrix <- function(x = matrix()){
      p <- NULL
      set <- function(y){
        x <<- y
        p <<- NULL
      }
      
      get <- function(x)
      setmatrix <- function(1/x) p <<- 1 / x
      getInverse <- function() p
      
}
